/* QTLogbook Light Theme - Stile chiaro con alto contrasto */

/* Stile generale dell'applicazione */
QWidget {
    font-family: 'Segoe UI', 'SF Pro Display', 'Helvetica Neue', Arial, sans-serif;
    font-size: 14px;
    color: #000000;
    background-color: #ffffff;
}

/* Stile per la finestra principale */
QMainWindow {
    background-color: #f8f9fa;
}

/* Stile per i titoli H1 (22pt) */
QLabel[class="h1"] {
    font-size: 22px;
    font-weight: 700;
    color: #000000;
    margin-bottom: 10px;
}

/* Stile per i titoli H2 (18pt) */
QLabel[class="h2"] {
    font-size: 18px;
    font-weight: 600;
    color: #000000;
    margin-bottom: 8px;
}

/* Stile per i widget centrali */
QMainWindow::central-widget {
    background-color: #ffffff;
    border: 1px solid #d0d0d0;
    border-radius: 6px;
}

/* Stile per le etichette */
QLabel {
    color: #000000;
    font-weight: 500;
    padding: 2px;
    font-size: 14px;
}

/* Stile per le etichette dei campi - H3 */
QLabel#fieldLabel {
    color: #000000;
    font-weight: 600;
    font-size: 16px; /* H3 */
}

/* Stile per i campi di testo */
QLineEdit {
    border: 1px solid #000000;
    border-radius: 4px;
    padding: 6px;
    background-color: #ffffff;
    selection-background-color: #0000ff;
    selection-color: #ffffff;
    font-size: 14px;
}

/* Stile specifico per il campo nominativo */
QLineEdit#callsignEdit {
    font-weight: 600;
    font-size: 16px; /* H3 */
    color: #0000ff;
}

QLineEdit:focus {
    border: 2px solid #0000ff;
}

QLineEdit:disabled {
    background-color: #f0f0f0;
    color: #505050;
}

/* Stile per i campi di sola lettura */
QLineEdit#readOnlyField {
    background-color: #f5f5f5;
    color: #000000;
    border: 1px solid #d0d0d0;
}

/* Stile per i menu a tendina */
QComboBox {
    border: 1px solid #000000;
    border-radius: 4px;
    padding: 5px 10px;
    background-color: #ffffff;
    selection-background-color: #0000ff;
    selection-color: #ffffff;
}

/* Stile specifico per i campi dropdown */
QComboBox#dropdownField {
    background-color: #f0f0ff;
    border: 1px solid #0000ff;
    min-width: 80px;
    max-width: 80px;
}

QComboBox:focus {
    border: 2px solid #0000ff;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: center right;
    width: 20px;
    border-left: none;
}

QComboBox::down-arrow {
    image: url(:/styles/down_arrow.svg);
    width: 12px;
    height: 12px;
    background-color: #505050;
}

/* Stile per i pulsanti */
QPushButton {
    background-color: #0000ff;
    color: #ffffff;
    border: none;
    border-radius: 4px;
    padding: 8px 16px;
    font-weight: 500;
    min-width: 80px;
    font-size: 14px;
}

QPushButton:hover {
    background-color: #0000cc;
}

QPushButton:pressed {
    background-color: #000099;
}

QPushButton:disabled {
    background-color: #cccccc;
    color: #505050;
}

/* Stile per il pulsante Clear */
QPushButton#clearButton {
    background-color: #f0f0f0;
    color: #000000;
    border: 1px solid #000000;
}

/* Stile per il pulsante Add */
QPushButton#addButton {
    background-color: #0000ff;
    color: #ffffff;
    font-weight: 600;
}

QPushButton#clearButton:hover {
    background-color: #e0e0e0;
}

QPushButton#clearButton:pressed {
    background-color: #d0d0d0;
}

QPushButton#addButton:hover {
    background-color: #0000cc;
}

QPushButton#addButton:pressed {
    background-color: #000099;
}

/* Stile per la tabella */
QTableView {
    border: 1px solid #000000;
    border-radius: 4px;
    background-color: #ffffff;
    alternate-background-color: #f5f5f5;
    gridline-color: #000000;
    selection-background-color: #e6f2ff;
    selection-color: #000000;
}

QTableView::item {
    padding: 4px;
    border-bottom: 1px solid #d0d0d0;
}

QTableView::item:selected {
    background-color: #e6f2ff;
    color: #000000;
    border: none;
}

QHeaderView::section {
    background-color: #f0f0f0;
    color: #000000;
    padding: 6px;
    border: none;
    border-right: 1px solid #000000;
    border-bottom: 1px solid #000000;
    font-weight: 500;
    font-size: 16px; /* H3 */
}

/* Stile per la barra di stato */
QStatusBar {
    background-color: #f8f9fa;
    color: #000000;
    border-top: 1px solid #d0d0d0;
}

/* Stile per la barra dei menu */
QMenuBar {
    background-color: #f8f9fa;
    border-bottom: 1px solid #d0d0d0;
    font-size: 14px;
}

QMenuBar::item {
    background-color: transparent;
    padding: 6px 10px;
}

QMenuBar::item:selected {
    background-color: #e6f2ff;
    color: #000000;
}

QMenu {
    background-color: #ffffff;
    font-size: 14px;
    border: 1px solid #000000;
    border-radius: 4px;
}

QMenu::item {
    padding: 6px 20px 6px 20px;
}

QMenu::item:selected {
    background-color: #e6f2ff;
    color: #000000;
}

/* Stile per i widget di data e ora */
QDateTimeEdit {
    border: 1px solid #000000;
    border-radius: 4px;
    padding: 6px;
    background-color: #ffffff;
}

QDateTimeEdit:focus {
    border: 2px solid #0000ff;
}

/* Stile per i dialoghi */
QDialog {
    background-color: #ffffff;
    border: 1px solid #d0d0d0;
    border-radius: 6px;
}

/* Stile per i messaggi di errore */
QMessageBox {
    background-color: #ffffff;
}

QMessageBox QLabel {
    color: #000000;
}

/* Stile per i tooltip - importante per l'accessibilità */
QToolTip {
    background-color: #ffffdc;
    color: #000000;
    border: 1px solid #000000;
    border-radius: 4px;
    padding: 4px;
    font-size: 12px;
}

/* Stile per lo stato di focus - importante per l'accessibilità */
*:focus {
    outline: 2px solid #0000ff;
    outline-offset: 1px;
}